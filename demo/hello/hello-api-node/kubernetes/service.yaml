apiVersion: v1
kind: Service
metadata:
  name: hello-api-node
  labels:
    app: hello-api-node
spec:
  selector:
    app: hello-api-node
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
